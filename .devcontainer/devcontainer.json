{
  "name": "Spring Boot + MySQL Dev Container",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "forwardPorts": [8080, 3306],
  "postCreateCommand": "chmod +x mvnw && sudo apt update && sudo apt install -y default-mysql-client docker.io && sudo usermod -aG docker vscode && sleep 20 && ./mvnw clean install || true"
}